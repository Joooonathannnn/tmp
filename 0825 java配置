原因找到了。Java 查看版本使用单横杠：

```powershell
java -version
```

而 Maven 使用双横杠或单横杠都可以：

```powershell
mvn -version
```

```powershell
mvn --version
```

现在进入项目目录构建即可：

```powershell
cd "你的目录\feedback-server"
mvn clean package
```

看到 `BUILD SUCCESS` 后，取出：

```text
target\feedback.war
```

上传到 Tomcat 服务器的：

```text
/opt/tomcat/webapps/feedback.war
```
