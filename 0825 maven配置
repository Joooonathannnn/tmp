根因已经明确：Maven 下载 `byte-buddy-1.12.19.jar` 时连接提前断开，留下的缓存文件可能不完整。不是项目代码错误。

先只清理这个依赖的缓存：

```powershell
$byteBuddyDir = "$env:USERPROFILE\.m2\repository\net\bytebuddy\byte-buddy\1.12.19"

Remove-Item -LiteralPath $byteBuddyDir -Recurse -Force
```

这只删除 Maven 的一个依赖缓存，不会删除项目源码。然后重新构建：

```powershell
cd "你的目录\feedback-server"
mvn -U clean package
```

如果依然卡在同一个文件，可以用浏览器从 Maven 官方仓库手动下载：

- [byte-buddy-1.12.19.jar](https://repo.maven.apache.org/maven2/net/bytebuddy/byte-buddy/1.12.19/byte-buddy-1.12.19.jar)
- [byte-buddy-1.12.19.pom](https://repo.maven.apache.org/maven2/net/bytebuddy/byte-buddy/1.12.19/byte-buddy-1.12.19.pom)

然后把两个文件放到：

```text
C:\Users\你的用户名\.m2\repository\net\bytebuddy\byte-buddy\1.12.19\
```

再次执行：

```powershell
mvn clean package
```

如果后续又有多个不同依赖出现同样的 `Premature end`，那就不是单个缓存问题，而是网络或公司代理持续截断 Maven 下载，需要使用公司内部 Maven 镜像或换一个网络环境构建。
