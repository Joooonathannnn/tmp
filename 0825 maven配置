“返回很多东西”大概率是接口返回了完整 JSON 数据，说明上传已经成功。先只看 HTTP 状态码：

```bash
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8080/feedback/api/dataset
```

如果返回：

```text
200
```

说明数据确实已经保存，只是可能保存到了 Tomcat 临时目录，而不是 `/var/lib/feedback`。

查找实际文件位置：

```bash
sudo find /var/lib/feedback /opt/tomcat/temp /tmp -maxdepth 4 -type f -name current.json -print
```

注意：我前面说默认一定是 `/tmp/feedback-data` 不够准确。Tomcat 通常会把 Java 临时目录设置成：

```text
/opt/tomcat/temp
```

因此文件可能位于：

```text
/opt/tomcat/temp/feedback-data/current.json
```

再检查环境变量：

```bash
sudo systemctl show tomcat --property=Environment
```

如果没有看到：

```text
FEEDBACK_DATA_DIR=/var/lib/feedback
```

说明数据目录配置没有生效，需要重新执行：

```bash
sudo systemctl edit tomcat
```

填写：

```ini
[Service]
Environment="FEEDBACK_DATA_DIR=/var/lib/feedback"
```

保存后执行：

```bash
sudo systemctl daemon-reload
sudo systemctl restart tomcat
```

暂时不要移动数据。先运行上面的 `find`，确定 `current.json` 实际在哪个路径。只需要告诉我它输出的那一条路径。
