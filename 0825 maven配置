现在已经生成部署文件了。下一步把 `feedback.war` 上传到服务器并交给 Tomcat 发布。

### 1. 在构建电脑找到 WAR

确保当前位于 `feedback-server`：

```powershell
Get-Item .\target\feedback.war
explorer .\target
```

里面的 `feedback.war` 就是要上传的文件。

### 2. 上传到服务器临时目录

在构建电脑 PowerShell 执行：

```powershell
scp ".\target\feedback.war" 服务器用户名@服务器IP:/tmp/feedback.war
```

例如：

```powershell
scp ".\target\feedback.war" testuser@10.10.10.10:/tmp/feedback.war
```

也可以使用 WinSCP，把文件上传到服务器的 `/tmp/feedback.war`。

### 3. 在服务器创建数据目录

登录服务器后执行：

```bash
sudo mkdir -p /var/lib/feedback/backups
sudo chown -R tomcat:tomcat /var/lib/feedback
sudo chmod 750 /var/lib/feedback
sudo chmod 750 /var/lib/feedback/backups
```

### 4. 配置 Tomcat 数据目录

执行：

```bash
sudo systemctl edit tomcat
```

写入：

```ini
[Service]
Environment="FEEDBACK_DATA_DIR=/var/lib/feedback"
```

保存退出，然后执行：

```bash
sudo systemctl daemon-reload
```

### 5. 备份原来的静态页面并部署 WAR

停止 Tomcat：

```bash
sudo systemctl stop tomcat
```

备份原来的 `feedback` 文件夹：

```bash
sudo mkdir -p /opt/tomcat/deploy-backup
sudo mv /opt/tomcat/webapps/feedback /opt/tomcat/deploy-backup/feedback-static-before-war
```

如果提示目标文件夹已经存在，先停止，不要覆盖。

部署 WAR：

```bash
sudo cp /tmp/feedback.war /opt/tomcat/webapps/feedback.war
sudo chown tomcat:tomcat /opt/tomcat/webapps/feedback.war
sudo chmod 640 /opt/tomcat/webapps/feedback.war
sudo systemctl start tomcat
```

### 6. 验证部署

查看 Tomcat 状态：

```bash
sudo systemctl status tomcat
```

检查健康接口：

```bash
curl -i http://127.0.0.1:8080/feedback/api/health
```

正常应返回：

```json
{"status":"UP"}
```

尚未上传数据时，下面接口返回 `404` 是正常的：

```bash
curl -i http://127.0.0.1:8080/feedback/api/dataset
```

最后在其他电脑浏览器访问：

```text
http://服务器IP:8080/feedback/
```

先完成到健康接口验证；只有返回 `{"status":"UP"}`，才说明服务器版已经正确启动。
