关键要看这次失败的 artifact 是否仍然是 `byte-buddy`。有可能 Byte Buddy 已经解决，现在下载另一个依赖时又被网络中断。

执行下面的命令，只提取最后一个失败依赖：

```powershell
mvn clean package 2>&1 |
  Tee-Object "$env:TEMP\maven-latest.txt"

(Select-String `
  -Path "$env:TEMP\maven-latest.txt" `
  -Pattern "Could not transfer artifact" |
  Select-Object -Last 1).Line
```

如果仍然是 `net.bytebuddy:byte-buddy:jar:1.12.19`

检查手动放入的文件名和完整性：

```powershell
$byteBuddyDir = "$env:USERPROFILE\.m2\repository\net\bytebuddy\byte-buddy\1.12.19"

Get-ChildItem -LiteralPath $byteBuddyDir |
  Select-Object Name, Length
```

文件名必须准确是：

```text
byte-buddy-1.12.19.jar
byte-buddy-1.12.19.pom
```

不能是：

```text
byte-buddy-1.12.19.jar.html
byte-buddy-1.12.19 (1).jar
byte-buddy-1.12.19.pom.txt
```

验证 JAR 是否完整：

```powershell
& "$env:JAVA_HOME\bin\jar.exe" tf `
  "$byteBuddyDir\byte-buddy-1.12.19.jar" |
  Select-Object -First 5
```

如果能显示 `META-INF` 等内容，JAR 基本完整。然后不要加 `-U`，直接运行：

```powershell
mvn clean package
```

如果已经变成另一个 artifact

说明手动放入的 Byte Buddy 已经生效，但网络又中断了其他依赖下载。这种情况下不适合逐个手动下载，根本解决方案是：

- 换到能够稳定访问 Maven Central 的网络环境；或
- 向公司同事获取内部 Maven 镜像地址并配置到 `settings.xml`。

先看最后一条 `Could not transfer artifact` 中的 artifact 名称是否还是 `byte-buddy`。
