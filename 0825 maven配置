如果还没有成功上传过数据，这是正常的：

- 第一次成功上传后，才会生成：

```text
/var/lib/feedback/current.json
```

- 第二次成功上传后，旧数据才会进入：

```text
/var/lib/feedback/backups/
```

所以 `backups` 第一次上传后仍为空。

现在进行一次上传：

1. 浏览器打开：

```text
http://服务器IP:8080/feedback/
```

2. 点击“下载模板”。
3. 点击“上传共享数据”，选择刚下载的 JSON。
4. 确认页面提示“上传成功”。

然后在服务器检查：

```bash
sudo ls -lh /var/lib/feedback/current.json
```

如果页面提示上传成功，但这里仍然没有文件，执行：

```bash
curl -i http://127.0.0.1:8080/feedback/api/dataset
sudo systemctl show tomcat --property=Environment
sudo ls -lh /tmp/feedback-data
```

判断方式：

- API 返回 `404 NO_DATA`：上传没有成功。
- API 返回 `200` 且 `/tmp/feedback-data/current.json` 存在：Tomcat 没加载 `FEEDBACK_DATA_DIR`。
- API 返回 `500 SAVE_FAILED`：通常是 `/var/lib/feedback` 权限不对。

权限可以检查为：

```bash
ls -ld /var/lib/feedback
```

应当显示所有者和用户组为：

```text
tomcat tomcat
```
