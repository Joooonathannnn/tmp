已经定位到原因：Tomcat 启动时没有读取到 `FEEDBACK_DATA_DIR`，所以程序使用了临时目录。需要修正环境变量并把现有数据复制到正式目录。

按顺序执行：

### 1. 停止 Tomcat

```bash
sudo systemctl stop tomcat
```

### 2. 把现有数据复制到正式目录

使用复制而不是移动，临时目录中仍保留一份：

```bash
sudo cp -p /opt/tomcat/temp/feedback-data/current.json /var/lib/feedback/current.json
sudo chown tomcat:tomcat /var/lib/feedback/current.json
sudo chmod 640 /var/lib/feedback/current.json
```

### 3. 重新配置环境变量

```bash
sudo systemctl edit tomcat
```

确保内容准确为：

```ini
[Service]
Environment="FEEDBACK_DATA_DIR=/var/lib/feedback"
```

保存退出，然后执行：

```bash
sudo systemctl daemon-reload
sudo systemctl start tomcat
```

### 4. 确认配置生效

```bash
sudo systemctl show tomcat --property=Environment
```

必须包含：

```text
FEEDBACK_DATA_DIR=/var/lib/feedback
```

检查接口：

```bash
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8080/feedback/api/dataset
```

应返回：

```text
200
```

检查正式数据：

```bash
sudo ls -lh /var/lib/feedback/current.json
```

### 5. 验证备份功能

在网页上再次上传一次 JSON，然后检查：

```bash
sudo ls -lh /var/lib/feedback/backups
```

这时应该出现一份备份文件。确认一切正常前，不要删除 `/opt/tomcat/temp/feedback-data/current.json`，它可以暂时作为恢复副本。
