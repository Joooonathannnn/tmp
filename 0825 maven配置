这表示 Maven 已经进入测试阶段，但某个测试失败或测试进程异常。仅凭这一行不能判断原因，暂时不要用 `-DskipTests` 跳过。

进入 `feedback-server` 目录，打开测试报告：

```powershell
explorer .\target\surefire-reports
```

里面应有三份 `.txt` 报告：

```text
com.example.feedback.DatasetValidatorTest.txt
com.example.feedback.DatasetStoreTest.txt
com.example.feedback.DatasetServletTest.txt
```

快速提取真正的错误：

```powershell
Select-String `
  -Path ".\target\surefire-reports\*.txt" `
  -Pattern "<<< FAILURE|<<< ERROR|AssertionError|Exception"
```

你只需要告诉我：

1. 哪个测试名称后面出现了 `<<< FAILURE` 或 `<<< ERROR`。
2. 它后面的第一条 `java.lang...` 或错误信息。

如果 `target\Surefire-reports` 不存在，则执行：

```powershell
mvn test 2>&1 |
  Tee-Object "$env:TEMP\feedback-test.txt"

Get-Content "$env:TEMP\feedback-test.txt" -Tail 30
```

`Failed to execute goal ... surefire ...` 只是汇总信息，真正原因一定在它前面或 `surefire-reports` 中。
