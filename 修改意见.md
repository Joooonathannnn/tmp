有，而且在你已经具备：

- Scale factor 搜索；
- SmoothQuant/放缩；
- 矩阵旋转；
- GPTQ；
- block-diagonal GPTQ；
- 2-pass 批处理近似；

之后，仍有几类值得尝试。对固定 HiF4 4.5-bit 结构，我最推荐的不是再叠一个通用 PTQ 名词，而是围绕 HiF4 的三级共享元数据做“格式感知优化”。

先说明一下：目前目录中只有两篇论文和提取材料，没有看到基线代码，所以我无法确认下面哪些已经隐含实现。如果把实现放进目录，我可以进一步逐项审计。

# 第一优先级：HiF4 三级元数据联合搜索

你目前的编码如果是：

1. 根据 64 个数确定 E6M2；
2. 根据局部最大值确定 8 个 lv2；
3. 根据更小局部最大值确定 16 个 lv3；
4. 对 64 个 S1P2 就近取整；

这是一个贪心过程。前一级一旦决定，后一级只能在它的基础上补救，但局部最大值并不一定对应全局最优编码。

## 1. 固定 E6M2 后，可以局部精确枚举

一个 8 元素组中包含：

- 1 个 lv2 位；
- 2 个 lv3 位；
- 两组各 4 个 S1P2 数值。

因此固定总 scale 后，一个 8 元素组的层级组合只有：

\[
2^1\times2^2=8
\]

种。

可以对每个 8 元素组枚举这 8 种组合：

1. 枚举 lv2；
2. 枚举两个 lv3；
3. 在该组合下让 8 个值选择最近的 S1P2；
4. 计算误差；
5. 选择误差最小的组合。

8 个大组彼此独立，因此不需要枚举全部：

\[
2^{8+16}=2^{24}
\]

种元数据组合，只需要：

\[
8\text{ 个组}\times8\text{ 种组合}
\]

再在若干 E6M2 候选上重复。

如果 E6M2 有 \(M\) 个候选，复杂度大致只有：

\[
M\times8\times8\times8
\]

数量级，非常可控。

## 2. 比局部最大值好在哪里

局部最大值规则隐含认为：

> 每个 4/8 元素组中的最大值最重要。

联合搜索可以发现另一种情况：

- 把 lv3 留给最大值，只改善一个数；
- 把 lv3 设成另一状态，虽然截断最大值，却改善另外三个数；
- 后者的总体误差可能更低。

这项优化：

- 不改变 HiF4 位布局；
- 不增加在线状态；
- 权重可以完全离线；
- 激活也能使用快速枚举；
- 与 Scale factor 搜索、GPTQ完全兼容。

这是我认为最应该先确认是否实现完整的一项。

---

# 第二优先级：量化输入感知的权重重构

普通 GPTQ经常优化：

\[
\min_{\hat W}
\|XW^T-X\hat W^T\|^2
\]

但你的在线输入也会被量化。真正执行的是：

\[
\hat Y=Q_H(X)\hat W^T
\]

所以更准确的权重目标应该是：

\[
\min_{\hat W}
\left\|
XW^T-Q_H(X)\hat W^T
\right\|^2
\]

这叫作“非对称重构”或“quantized-input-aware reconstruction”。

## 为什么可能比继续增强 GPTQ 更有效

完整误差包含：

\[
Y-\hat Y
=
XW^T-(X+\Delta X)(W+\Delta W)^T
\]

展开后有：

\[
-\Delta XW^T
-X\Delta W^T
-\Delta X\Delta W^T
\]

普通权重 GPTQ 重点处理 \(X\Delta W^T\)，但没有充分处理：

- 激活量化误差；
- 激活误差与权重误差之间的相互作用。

权重量化时如果使用已经量化的校准激活，可能让权重误差主动抵消一部分激活误差。

具体流程是：

1. 使用最终在线算法量化校准激活：

\[
\hat X_{\mathrm{cal}}=Q_H(X_{\mathrm{cal}})
\]

2. 目标仍使用全精度输出：

\[
Y_{\mathrm{ref}}=X_{\mathrm{cal}}W^T
\]

3. 搜索 HiF4 权重：

\[
\hat W^*
=
\arg\min_{\hat W\in\mathrm{HiF4}}
\|Y_{\mathrm{ref}}-\hat X_{\mathrm{cal}}\hat W^T\|^2
\]

4. 在 GPTQ、2-pass 和 Scale factor 搜索中统一使用这个目标。

这种非对称重构也出现在 AdaRound 一类方法中。[AdaRound 原论文](https://arxiv.org/abs/2004.10568)

---

# 第三优先级：Attention 的 K 均值消除

这是一个很适合你的 Attention 数据、而且数学上非常漂亮的优化。

设每个 attention head 的 K 通道均值为：

\[
\mu_K=\frac1N\sum_{j=1}^N K_j
\]

定义：

\[
K'=K-\mathbf1\mu_K
\]

QK logits 变成：

\[
QK'^T
=
QK^T-Q\mu_K^T\mathbf1^T
\]

对某一个 query 来说，第二项是加到所有 key logits 上的同一个常数。由于 softmax 满足：

\[
\operatorname{softmax}(z+c\mathbf1)
=
\operatorname{softmax}(z)
\]

所以：

\[
\operatorname{softmax}(QK'^T)
=
\operatorname{softmax}(QK^T)
\]

也就是说：

> K 均值消除在未量化时不改变 attention 概率，却可能显著缩小 K 的动态范围。

这比一般旋转更直接，在线只需要：

- 求均值；
- 做减法；
- 然后动态量化 K。

SageAttention 系列使用 Q/K smoothing 改善低比特 Attention，官方实现也将 smoothing 和不同粒度的 Q/K 量化作为核心能力。[SageAttention3 论文](https://arxiv.org/abs/2505.11594)、[官方代码库](https://github.com/thu-ml/SageAttention)

## V 也可以做均值消除

令：

\[
\mu_V=\frac1N\sum_jV_j,\qquad V'=V-\mathbf1\mu_V
\]

由于 attention 概率每行之和为 1：

\[
PV'=PV-\mathbf1\mu_V
\]

因此只要输出端补回：

\[
O=PV'+\mathbf1\mu_V
\]

就能严格恢复全精度结果。

V centering 是否值得做，需要比较：

- 减均值带来的量化改善；
- 在线求均值和输出补偿成本。

---

# 第四优先级：通道重排与幅值聚类

旋转会混合通道；重排只改变通道顺序。

核心思想是：

> 把幅值范围接近的通道排在一起，使它们尽量进入同一个 HiF4 64-value 块。

例如通道范围：

\[
[1,80,2,75,1.5,90,2.2,60]
\]

交错排列会让每个块都包含异常值。重新排列为：

\[
[1,1.5,2,2.2,60,75,80,90]
\]

可以让小值块使用较小 scale，大值块使用较大 scale。

RPTQ 的核心发现是，激活量化问题不仅来自异常值，也来自不同通道之间的范围差异，因此通过通道重排和聚类缓解共享 scale 冲突。[RPTQ](https://arxiv.org/abs/2304.01089)

## 怎样适配 HiF4

校准阶段为每个通道计算特征，例如：

\[
r_j=
\operatorname{percentile}_{99.9}(|X_j|)
\]

或者同时使用：

\[
f_j=
[
\log \max|X_j|,
\log\operatorname{RMS}(X_j),
\text{kurtosis}(X_j)
]
\]

根据这些特征排序或聚类，让相近通道组成 64-value 块。

Linear 中需要同步变换：

\[
X'=XP,\qquad W'=WP
\]

其中 \(P\) 是置换矩阵，因此：

\[
X'W'^T=XW^T
\]

Attention 中则可以对 Q/K 使用相同置换：

\[
Q'=QP,\qquad K'=KP
\]

仍然保证：

\[
Q'K'^T=QK^T
\]

重排的主要优势是：

- 不需要浮点乘法；
- 只改变地址或布局；
- 容易离线吸收到权重；
- 特别适合固定 64-value 分组。

如果你已有的“矩阵变换”只包含放缩和 Hadamard，而没有数据驱动的通道重排，这一项值得单独加入。

---

# 第五优先级：自适应舍入，而不是逐点最近舍入

GPTQ 会补偿权重误差，但最底层的 HiF4 编码可能仍然是“离哪个 S1P2 格点近就选哪个”。

最近舍入不一定使算子输出误差最小。

假设某个归一化值位于两个 S1P2 候选之间：

\[
x\in[q_{\mathrm{low}},q_{\mathrm{high}}]
\]

普通量化按中点决定：

\[
x<\frac{q_{\mathrm{low}}+q_{\mathrm{high}}}{2}
\Rightarrow q_{\mathrm{low}}
\]

自适应舍入则允许：

- 当前值虽然更接近 \(q_{\mathrm{low}}\)；
- 但为了抵消其他权重误差，选择 \(q_{\mathrm{high}}\)；
- 最终 Linear 输出反而更准确。

AdaRound 证明了任务或输出感知的舍入可以优于逐点最近舍入。[AdaRound](https://arxiv.org/abs/2004.10568)

## HiF4-aware AdaRound

不能只学习每个值“向上还是向下”，还可以学习：

- E6M2 候选概率；
- lv2 位的软选择；
- lv3 位的软选择；
- S1P2 相邻格点的软选择。

校准时先使用 soft relaxation：

\[
\hat x
=
p q_{\mathrm{low}}+(1-p)q_{\mathrm{high}}
\]

然后逐渐把 \(p\) 推向 0 或 1，最终导出完全合法的 HiF4 比特。

它不增加最终存储位数，但离线优化成本会比 GPTQ 和枚举更高。

---

# 第六优先级：可学习的 HiF4 参数联合校准

OmniQuant 的思路是，不手工确定所有量化参数，而是用校准数据联合学习：

- clipping；
- 等价放缩；
- 量化参数。

其两个主要组件是 Learnable Weight Clipping 和 Learnable Equivalent Transformation。[OmniQuant](https://arxiv.org/abs/2308.13137)

你的版本可以做成：

\[
\Theta=
\{
D,\ c,\ \tau,\ p_{\mathrm{lv2}},p_{\mathrm{lv3}},
p_{\mathrm{round}}
\}
\]

其中：

- \(D\)：SmoothQuant 通道放缩；
- \(c\)：Scale factor；
- \(\tau\)：截断比例；
- \(p_{\mathrm{lv2}}\)、\(p_{\mathrm{lv3}}\)：层级位的软选择；
- \(p_{\mathrm{round}}\)：舍入方向。

优化：

\[
\min_\Theta
\left\|
F_{\mathrm{FP}}(X,W)
-
F_{\mathrm{HiF4}}(X,W;\Theta)
\right\|^2
\]

最后把所有软变量离散化，仍输出严格的：

- 1 个 E6M2；
- 8 个 lv2；
- 16 个 lv3；
- 64 个 4-bit 数值。

这可以看作“HiF4-OmniQuant”。

---

# 第七优先级：输出 Bias Compensation

权重量化后，Linear 输出误差可能具有稳定均值：

\[
e_n
=
X_nW^T-\hat X_n\hat W^T
\]

在校准集上计算：

\[
b_{\mathrm{corr}}
=
\frac1N\sum_ne_n
\]

在线输出增加：

\[
\hat Y_{\mathrm{corr}}
=
\hat X\hat W^T+b_{\mathrm{corr}}
\]

这不能消除随机误差，但可以消除系统性偏差。

它的优势是：

- 校准阶段有闭式解；
- 不需要修改 HiF4 权重编码；
- 在线只增加逐输出通道加法；
- 可以吸收到已有 Linear bias 中。

Bias Compensation 已被用于直接最小化量化后的输出偏差。[Bias Compensation](https://arxiv.org/abs/2404.01892)

限制是：

- 如果赛题严格禁止额外 bias 状态，就不能用；
- 如果允许“校准阶段保存量化相关参数”，通常可以保存；
- 应确认评分是否把额外状态计入 4.5-bit 预算。

---

# 第八优先级：Attention 联合 Q/K Scale 搜索

现在很多动态量化器是分别做：

\[
\hat Q=Q_{c_Q}(Q)
\]

\[
\hat K=Q_{c_K}(K)
\]

分别最小化：

\[
\|Q-\hat Q\|^2,\qquad
\|K-\hat K\|^2
\]

但 Q/K 真正共同决定的是：

\[
S=QK^T
\]

所以可以联合搜索：

\[
(c_Q^*,c_K^*)
=
\arg\min_{c_Q,c_K}
\left\|
QK^T-
Q_{c_Q}(Q)Q_{c_K}(K)^T
\right\|^2
\]

如果各有 4 个 Scale factor 候选，联合也只有：

\[
4\times4=16
\]

种。

进一步可以直接优化 softmax 后误差：

\[
L=
\lambda_1
\operatorname{NMSE}(S,\hat S)
+
\lambda_2
D_{\mathrm{KL}}(P\|\hat P)
+
\lambda_3
\operatorname{NMSE}(PV,\hat P\hat V)
\]

这比“分别量化 Q 和 K”更符合 Attention 的真实误差路径。

---

# 第九优先级：学习动态激活的舍入阈值

权重可以离线做 AdaRound，但激活和 QKV 每次都变化，不能保存逐元素舍入决策。

不过可以学习少量共享阈值。

普通最近舍入的边界是相邻格点中点：

\[
t_i=\frac{q_i+q_{i+1}}2
\]

可以改为：

\[
t_i' = t_i+\delta_i
\]

其中 \(\delta_i\) 由校准数据学习，并按以下粒度共享：

- 每层一套；
- Q/K/V 各一套；
- 每个 attention head 一套；
- 每种 lv2/lv3 状态一套。

在线仍然只是比较阈值，不需要搜索或反向传播。

这类方法的价值在于：

- 修正“就近舍入产生的系统偏差”；
- 不改变 HiF4 输出结构；
- 状态量很小；
- 比在线自适应舍入便宜。

---

# 第十优先级：更稳健的校准目标

这不是新量化器，但经常决定优化在测试集上是否真正有效。

不要只使用校准集平均 MSE：

\[
L_{\mathrm{mean}}
=
\frac1N\sum_nL_n
\]

可以改为：

\[
L=
\operatorname{mean}(L_n)
+
\beta\operatorname{percentile}_{95}(L_n)
+
\gamma L_{\mathrm{worst-group}}
\]

这样不会为了少数常见样本，把某些罕见但严重的块完全牺牲掉。

同时建议：

- 将校准集划分为参数搜索集和校准验证集；
- 不使用测试集选择超参数；
- 对激活统计使用 percentile/RMS，而不是只使用绝对最大值；
- 限制 Scale factor 偏离 1 的程度；
- 记录每层、每 head 的最坏误差，而不只看全局均值。

---

# 哪些方法不适合你的严格约束

有些量化方法理论上有效，但可能破坏严格 4.5-bit 结构。

| 方法 | 不适合的原因 |
|---|---|
| 稀疏异常值残差 | 需要额外保存异常值位置和高精度数值 |
| 混合精度敏感通道 | 平均位宽不再严格是 4.5 bit |
| 低秩 LoRA/SVD 残差 | 需要额外矩阵和计算 |
| 学习新 codebook | 不再使用规定的 S1P2 编码 |
| 每块额外零点 | 增加元数据 |
| 每样本保存最优 Scale factor | 可能增加在线状态或超出格式 |
| FP16 fallback | 可能违反所有输入必须 HiF4 的规则 |

这些只有在赛题允许额外状态或只限制主数据格式时才值得考虑。

另外，不建议把随机舍入直接用于推理阶段的激活、Q/K/V。你目录中的 NVFP4 论文报告，随机舍入对训练梯度有用，但用于前向权重和激活反而可能增大量化误差。[NVFP4 论文](https://arxiv.org/pdf/2509.25149)

# 我建议的实际优先级

如果现有方法确实都已实现，我建议下一轮按这个顺序做消融：

1. **三级元数据精确/束搜索**；
2. **使用量化校准输入的非对称权重重构**；
3. **Attention 的 K mean subtraction**；
4. **Q/K Scale factor 联合搜索**；
5. **64-value 对齐的通道重排/聚类**；
6. **HiF4-aware adaptive rounding**；
7. **Bias compensation**；
8. **完整的可学习联合校准**。

其中最可能以较低风险带来收益的是前三项：

- 三级元数据联合搜索充分利用 HiF4 格式；
- 非对称重构直接处理激活与权重的联合误差；
- K 均值消除针对 Attention，而且未量化时严格保持 softmax 不变。

近期 microscaling FP4 研究也反复显示：通用 INT4 PTQ 技术并不一定直接迁移到 FP4，格式特定的 scale、分组和变换策略更重要。[MR-GPTQ](https://arxiv.org/abs/2509.23202)、[MXFP PTQ Benchmark](https://arxiv.org/abs/2601.09555)。
