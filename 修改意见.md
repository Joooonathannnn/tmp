你现在的方案已经比普通 GPTQ 更进一步了：

- 初始阶段用独立 lv3 + 7 倍率快速得到合法 HiF4 编码；
- 后处理通过协方差把强相关通道组成 4 元组；
- 每组枚举 \(3^4=81\) 个联合偏移；
- 用完整 2048-D metric 做 Gauss–Seidel 更新。

它已经能解决“单通道改变会变差，但四个通道一起改变会变好”的舍入壁垒。下一步最值得改进的不是扩大搜索空间，而是：

> 保留完整全局 metric，但把候选评估改成增量计算；然后用节省的时间增加重叠分组和难块元数据逃逸。

# 1. 首选：全局 metric 的精确增量计算

假设当前某一行权重的量化误差为：

\[
e=\hat w-w
\]

全局 metric 是：

\[
L=e^THe
\]

其中 \(H\) 是校准激活协方差或 Hessian 近似。

现在修改 4 个通道 \(G=\{i_1,i_2,i_3,i_4\}\)，产生偏移：

\[
\delta_G\in\{-0.25,0,+0.25\}^4
\]

新的误差为：

\[
e'=e+U_G\delta_G
\]

不需要对每个候选重新计算完整 2048-D metric。令：

\[
g=He
\]

则损失变化可以精确写成：

\[
\Delta L
=
2\delta_G^Tg_G+
\delta_G^TH_{GG}\delta_G
\]

其中：

- \(g_G\) 只有 4 维；
- \(H_{GG}\) 只有 \(4\times4\)；
- \(\delta_G\) 有 81 个候选。

因此 81 个候选可以一次性计算：

\[
\Delta L_c
=
2\delta_c^Tg_G+
\delta_c^TH_{GG}\delta_c,
\qquad c=1,\ldots,81
\]

选中最优候选后，只需更新：

\[
e_G\leftarrow e_G+\delta_G
\]

\[
g\leftarrow g+H_{:,G}\delta_G
\]

\[
L\leftarrow L+\Delta L
\]

## 这为什么重要

它没有近似，仍然使用完整 2048-D metric，但候选评估从全维计算变成了 4 维二次型。

每个 group 的主要成本变为：

- 81 个 \(4\times4\) 小二次型；
- 接受候选后一次 \(2048\times4\) 的梯度更新。

如果你当前是为每个候选重新构造完整误差或计算完整二次型，这一项可能带来非常明显的加速。

前提是你的 metric 确实是固定 \(H\) 下的二次型；如果还包含 softmax 等非线性，只能把它作为候选预筛选，最后再用真实 metric 验证。

---

# 2. 把 81 个候选完全向量化

预先构造固定候选矩阵：

\[
D\in\mathbb{R}^{81\times4}
\]

其中每一行是：

\[
[-0.25,0,+0.25]^4
\]

对一个 group，一次计算：

\[
s=
2Dg_G+
\operatorname{diag}(DH_{GG}D^T)
\]

不需要对 81 个候选使用循环。

如果实际偏移还要乘每个通道对应的 scale：

\[
\delta_{c,j}=D_{c,j}\cdot s_j
\]

也可以广播完成。

需要把非法候选设成 \(+\infty\)，例如：

- S1P2 超出范围；
- 改变后不满足当前 lv2/lv3；
- 符号或饱和规则不合法。

另外，\(H_{GG}\) 对所有输出行相同，可以复用；不同权重行只需要替换 \(g_G\) 和实际量化步长。

---

# 3. 当前最大的问题：固定的 4 通道分组

现在按协方差聚成不相交的四元组。这样能利用组内相关性，但会忽略组间强相关关系。

例如：

```text
Group A = [1, 7, 20, 35]
Group B = [5, 9, 42, 61]
```

如果通道 35 与通道 42 强相关，它们永远不能在同一次 81 候选搜索中联合移动。

## 改进：第二轮重叠 Schwarz 分组

第一轮仍使用原来的协方差聚类。

第一轮结束后，计算当前梯度：

\[
g=He
\]

以及跨组相关收益，例如：

\[
A_{ij}
=
|H_{ij}|\cdot s_i s_j\cdot a_i a_j
\]

其中：

- \(s_i,s_j\)：对应通道的实际量化步长；
- \(a_i,a_j\)：通道当前是否“可移动”的指标；
- \(H_{ij}\)：两个通道之间的 metric 耦合。

然后从不同旧 group 中选择强耦合通道，组成少量新的重叠 group。

建议：

- 第一轮：所有 group；
- 第二轮：只重组收益最高的 5%～10% 通道；
- 不做第二次全量聚类；
- 只接受 \(\Delta L<0\) 的更新。

这能解决固定分区造成的新舍入壁垒，通常比把 group 从 4 扩大到 5 或 6 更划算。

---

# 4. 聚类不应只看协方差

只用：

\[
|H_{ij}|
\]

聚类，可能把高度相关、但根本不值得移动的通道放在一起。

例如：

- 两个通道协方差很大；
- 但当前量化已经非常准确；
- 或者它们处于 S1P2 边界，无法向需要的方向移动。

更合适的 affinity 可以加入量化步长和当前残差：

\[
A_{ij}
=
\frac{|H_{ij}|}{\sqrt{H_{ii}H_{jj}}+\epsilon}
\cdot
s_i s_j
\cdot
u_i u_j
\]

其中 \(u_i\) 表示通道的“优化潜力”，可以取：

\[
u_i=|g_i|s_i
\]

或者结合：

- 当前量化误差；
- 距离相邻 code 的距离；
- 是否饱和；
- 上移、下移是否合法。

直观上：

> 不仅要找相关通道，还要找当前确实有联合移动价值的相关通道。

如果分组必须对所有权重行共享，可以对输出行聚合：

\[
u_i=
\operatorname{mean}_r
\left(
|g_{r,i}|s_{r,i}
\right)
\]

或使用高分位数，避免被少量异常行控制。

---

# 5. 自适应 group size

不是所有 group 都值得使用 \(3^4=81\) 搜索。

可以分成三档：

| 类型 | 搜索方式 | 候选数 |
|---|---|---:|
| 简单通道 | 单通道或不搜索 | 3 |
| 中等耦合 | 2 通道联合 | \(3^2=9\) |
| 强耦合难组 | 4 通道联合 | \(3^4=81\) |

判断依据可以是组内非对角耦合：

\[
\rho_G
=
\frac{
\sum_{i\ne j}|H_{ij}|s_is_j
}{
\sum_iH_{ii}s_i^2+\epsilon
}
\]

- \(\rho_G\) 小：各通道接近独立，使用 81 候选没有多少价值；
- \(\rho_G\) 中等：pair search 足够；
- \(\rho_G\) 大：保留 Schwarz4。

一种保守配置是：

- 60%～70% 通道只做 pair 或跳过；
- 20%～30% 做四元组；
- 最难的 5%～10% 进入第二轮重叠四元组。

这不仅节省时间，还能把预算集中到真正存在联合舍入壁垒的位置。

---

# 6. Active-set Gauss–Seidel

普通循环式 Gauss–Seidel 每一轮都会重新访问所有 group，但后期绝大多数 group 的最优候选通常已经是：

\[
\delta_G=0
\]

这些计算没有收益。

可以维护 active set：

1. 第一轮访问所有 group；
2. 记录每组最佳收益：

\[
r_G=-\min_c\Delta L_{G,c}
\]

3. 第二轮只访问满足以下条件的 group：

\[
r_G>\tau
\]

4. 某组更新后，只重新激活与它具有较大 \(H_{ij}\) 耦合的邻居；
5. 连续两次最优候选都是零偏移，则冻结。

这类似稀疏坐标下降，可以减少大量无效的后期 sweep。

还可以使用“lazy priority”：

- 优先访问预测收益最大的 group；
- 访问时重新计算其真实收益；
- 收益低于阈值则丢弃；
- 时间到就直接返回当前最优结果。

这比固定做两轮或三轮更适合你的 300 秒硬限制。

---

# 7. 信赖域扩展：避免永远只能移动 ±0.25

当前每轮只允许：

\[
\{-0.25,0,+0.25\}
\]

如果某个 group 的最优候选位于搜索边界，例如：

\[
[+0.25,+0.25,0,-0.25]
\]

而且收益仍然很大，说明真正的最优点可能还在这个方向的更远处。

不建议直接扩成：

\[
\{-0.5,-0.25,0,+0.25,+0.5\}^4
\]

因为候选数会变成：

\[
5^4=625
\]

更经济的办法是：

1. 先运行正常 81 候选；
2. 如果最优解触及边界且收益超过阈值；
3. 接受这一步；
4. 仅对该 group 再运行一次 81 候选。

这样相当于允许选择 \(\pm0.5\)，但第二步只发生在少量困难 group 上。

---

# 8. 从“全精度输入 metric”升级为非对称重构 metric

如果当前 metric 是：

\[
L=
(XW^T-X\hat W^T)^2
\]

它只优化权重量化误差。但实际在线阶段激活也是 HiF4：

\[
\hat Y=Q_H(X)\hat W^T
\]

更符合真实结果的目标是：

\[
L=
\left\|
XW^T-\hat X\hat W^T
\right\|^2,
\qquad
\hat X=Q_H(X)
\]

展开为关于 \(\hat w\) 的二次函数：

\[
L(\hat w)
=
\hat w^T H_q\hat w
-2b^T\hat w
+\mathrm{const}
\]

其中：

\[
H_q=\hat X^T\hat X
\]

\[
b=\hat X^Ty_{\mathrm{ref}}
\]

因此梯度不再只是：

\[
H(\hat w-w)
\]

而是：

\[
g=H_q\hat w-b
\]

候选增量仍可精确计算：

\[
\Delta L
=
2\delta_G^Tg_G+
\delta_G^T(H_q)_{GG}\delta_G
\]

所以现有 Schwarz4 结构几乎不用改变，却能让权重量化结果补偿一部分激活量化误差。

这可能比继续扩大候选数量更有价值。

---

# 9. Hessian/协方差需要适度正则化

完整 2048-D 校准协方差可能由于校准样本不足而过拟合。

建议使用：

\[
H_\lambda
=
(1-\lambda)H+
\lambda\operatorname{diag}(H)
+
\epsilon I
\]

效果是：

- 保留大部分通道相关性；
- 降低不可靠的跨通道协方差；
- 改善 Gauss–Seidel 数值稳定性；
- 减少校准集上很好、测试集上变差的情况。

\(\lambda\) 不需要密集搜索，可以只比较：

\[
\lambda\in\{0,0.05,0.1\}
\]

或者根据校准样本数自动设置。

---

# 10. 只对极难块搜索元数据邻域

Schwarz4 当前只改变 code：

\[
q_i\rightarrow q_i+\{-0.25,0,+0.25\}
\]

如果真正的壁垒来自：

- lv3 选择错误；
- lv2 选择错误；
- E6M2/Scale factor 不合适；

再多 code 微调也跨不过去。

但没必要全量做三级搜索。可以只对 Schwarz4 后残差最大的 0.5%～1% HiF4 块，增加少量候选：

- 当前元数据；
- 翻转误差最大的一个 lv3；
- 翻转对应 lv2；
- E6M2 相邻一个格点；
- Scale factor 相邻一个候选。

每个元数据候选先重新生成合法 code，再用同一个全局二次增量 metric 评价。

这相当于：

> Schwarz4 负责跨 code 舍入壁垒，少量 metadata escape 负责跨层级壁垒。

---

# 11. 适合 300 秒的推荐版本

我建议组合成下面这个版本。

## Pass 0：初始量化

保留：

- 独立 lv3；
- 7 倍率；
- 当前快速编码。

## Pass 1：自适应 Schwarz

- 使用改进后的 covariance + step + gradient affinity 分组；
- 弱耦合组使用 \(3^2=9\) pair search；
- 强耦合组使用 \(3^4=81\) Schwarz4；
- 使用精确增量二次型一次性评估候选；
- 接受后增量更新全局梯度。

## Pass 2：选择性重叠 Schwarz

- 只处理 Pass 1 后收益最大的 5%～10% 通道；
- 跨原 group 重新配组；
- 最多一次 sweep；
- 只接受严格降低完整 metric 的候选。

## Pass 3：少量 metadata escape

- 只处理残差最高的 0.5%～1% HiF4 块；
- 尝试 lv3/lv2/相邻 scale 的少量候选；
- 候选内部再做一次 Schwarz4。

## 时间控制

- 240 秒后停止进入新重叠 group；
- 270～280 秒停止所有新搜索；
- 剩余时间用于最终编码与校验；
- 始终保存当前最好合法结果。

---

# 优先级结论

如果只做三项，我建议按这个顺序：

1. **把完整 2048-D metric 改成精确增量二次型**  
   基本不损失质量，可能显著节省候选评估时间。

2. **加入量化输入感知的非对称 metric**  
   不扩大 81 候选空间，但目标更接近真实 W4A4 Linear 输出。

3. **加入小比例重叠 Schwarz 分组**  
   解决固定四元组之间仍然存在的联合舍入壁垒。

你当前算法的核心方向是合理的。最大的潜在改进点不是把 `3^4` 变成 `5^4` 或扩大 group，而是让“谁组成四元组、哪些四元组值得继续搜索、如何低成本计算完整 metric”变得更聪明。




-------------------------------------------------------------------------------------------
结论先说：

> 如果第 1 项真正把“每个候选重新算完整 2048-D metric”改成增量二次型，那么加入三项后，整体时间大概率不会上升，反而可能下降。第 2 项主要增加一次校准矩阵预计算；第 3 项若只覆盖 5%～10% group，只增加约 5%～10% 的搜索成本。

但这取决于你当前 `refine_hif4_schwarz4_full_metric` 是否已经隐式使用了增量公式。如果已经用了，第 1 项不会再加速。

下面用：

- 通道维度 \(D=2048\)；
- Schwarz group size \(b=4\)；
- 每组候选数 \(C=3^4=81\)；
- group 数量：

\[
G=D/b=512
\]

- 输出通道数 \(R\)；
- 校准样本数 \(N\)；

来分析。

# 1. 精确增量二次型

## 当前可能的复杂度

如果每个 group 的每个候选都重新计算：

\[
L_c=e_c^THe_c
\]

完整稠密二次型成本是：

\[
O(D^2)
\]

那么一轮 Schwarz 的复杂度为：

\[
O\left(RGCD^2\right)
\]

代入 \(G=D/b\)：

\[
O\left(\frac{RC D^3}{b}\right)
\]

这非常高，正常实现一般不会真的为 81 个候选重复做完整矩阵乘法。

如果当前已经预先算出 \(g=He\)，但每个候选仍然构造 2048-D 误差向量并进行全维评分，那么每个候选约为：

\[
O(D)
\]

一轮为：

\[
O(RGCD)
=
O\left(\frac{RC D^2}{b}\right)
\]

## 改成精确增量后的复杂度

对一个四元组：

\[
\Delta L_c
=
2\delta_c^Tg_G+
\delta_c^TH_{GG}\delta_c
\]

每个候选只处理 \(b=4\) 维：

\[
O(b^2)
\]

81 个候选：

\[
O(Cb^2)
\]

接受候选后更新全局梯度：

\[
g\leftarrow g+H_{:,G}\delta_G
\]

成本：

\[
O(Db)
\]

因此单个 group 的成本变成：

\[
O(Cb^2+Db)
\]

一轮全部 group：

\[
O\left(
G(Cb^2+Db)
\right)
\]

代入 \(G=D/b\)：

\[
O(CbD+D^2)
\]

加上初始化：

\[
g=He
\]

需要一次：

\[
O(D^2)
\]

所以每个输出行的一轮总复杂度约为：

\[
O(D^2+CbD)
\]

由于 \(b=4,C=81\) 都是常数，主项是：

\[
O(D^2)
\]

全部输出行为：

\[
O(RD^2)
\]

## 代入 2048 维

每个输出行：

### 初始化 \(g=He\)

\[
2048^2\approx4.19\text{M}
\]

次乘加。

### 512 个 group 的候选评分

\[
512\times81\times4^2
\approx0.66\text{M}
\]

次小规模运算。

### 假设每个 group 都接受更新

\[
512\times2048\times4
\approx4.19\text{M}
\]

次乘加。

合计约：

\[
4.19+0.66+4.19
\approx9.04\text{M}
\]

次运算/输出行。

如果只有一部分 group 接受非零更新，实际会更低。

## 与全维候选评分比较

如果当前每个候选都做一次 2048 维评分：

\[
512\times81\times2048
\approx84.9\text{M}
\]

而增量版本候选评分与梯度更新合计约：

\[
0.66+4.19=4.85\text{M}
\]

在这一部分理论上减少约：

\[
17.5\times
\]

但总程序不一定加速 17 倍，因为还有：

- 数据读取；
- 初始量化；
- 协方差计算；
- 聚类；
- GPTQ；
- 编码输出；
- GPU launch 和内存访问。

---

# 2. 量化输入感知的非对称 metric

原来的对称目标一般是：

\[
L_{\mathrm{sym}}
=
\|XW^T-X\hat W^T\|^2
\]

对应：

\[
H=X^TX
\]

以及：

\[
g=H(\hat w-w)
\]

非对称目标改为：

\[
L_{\mathrm{asym}}
=
\|XW^T-\hat X\hat W^T\|^2
\]

其中：

\[
\hat X=Q_{\mathrm{HiF4}}(X)
\]

令：

\[
H_q=\hat X^T\hat X
\]

\[
B=\hat X^TY_{\mathrm{ref}}
\]

其中：

\[
Y_{\mathrm{ref}}=XW^T
\]

梯度变成：

\[
g=H_q\hat W^T-B
\]

## 搜索阶段复杂度不变

Schwarz 候选损失变化仍然是：

\[
\Delta L_c
=
2\delta_c^Tg_G+
\delta_c^T(H_q)_{GG}\delta_c
\]

所以：

- 仍然是 81 个候选；
- 仍然是四维二次型；
- 仍然使用相同增量梯度更新；
- 不会扩大搜索空间。

搜索复杂度仍然是：

\[
O(RD^2+RCbD)
\]

## 增加的是校准预计算

需要额外计算：

### 量化校准输入

\[
\hat X=Q_H(X)
\]

复杂度：

\[
O(ND)
\]

通常不大。

### 量化输入协方差

\[
H_q=\hat X^T\hat X
\]

复杂度：

\[
O(ND^2)
\]

如果它替换原来的 \(X^TX\)，而不是两者都保留，那么渐进复杂度不变，只是输入矩阵换成了 \(\hat X\)。

### 非对称目标项

\[
B=\hat X^TY_{\mathrm{ref}}
\]

复杂度：

\[
O(NDR)
\]

这是主要新增成本，相当于一次矩阵乘法：

\[
[D,N]\times[N,R]
\]

如果 \(R\approx D\)，它与构造一个 \(D\times D\) 协方差矩阵的成本处于同一量级。

## 如何降低这部分成本

不必一次保存完整 \(B\)。可以按输出行或输出通道 chunk 计算：

\[
B_{\mathrm{chunk}}
=
\hat X^TY_{\mathrm{ref,chunk}}
\]

然后立即对该 chunk 运行 Schwarz，避免增加大量内存。

也可以直接通过当前输出残差初始化梯度：

\[
R_q=\hat X\hat W^T-Y_{\mathrm{ref}}
\]

\[
g=\hat X^TR_q
\]

代价仍是 GEMM，但更容易按 chunk 执行。

## 预期时间影响

如果协方差和目标矩阵计算占原 180 秒的比例较小，非对称 metric 可能只增加几秒到几十秒。

如果 \(N,R,D\) 都很大，额外的 \(B=\hat X^TY\) 可能接近增加一次大型 GEMM，但通常仍比 81 候选反复做全维评分便宜。

---

# 3. 小比例重叠 Schwarz 分组

假设基础一轮包含：

\[
G=512
\]

个 group。

第二轮只增加比例 \(p\) 的重叠 group，则增加：

\[
G_{\mathrm{overlap}}=pG
\]

例如：

- \(p=5\%\)：约 26 个新 group；
- \(p=10\%\)：约 51 个新 group。

额外搜索复杂度：

\[
O\left(
pG(Cb^2+Db)
\right)
\]

也就是基础 Schwarz sweep 的约 \(p\) 倍。

因此：

- 5% 重叠分组：搜索部分约增加 5%；
- 10% 重叠分组：搜索部分约增加 10%；
- 20% 重叠分组：搜索部分约增加 20%。

不过基础 sweep 还包含一次 \(g=He\) 初始化，而重叠轮可以复用现有 \(g\)，所以实际增加比例通常小于 \(p\)。

## 2048 维、10% 重叠的额外运算

约 51 个 group。

### 候选评分

\[
51\times81\times16
\approx66\text{K}
\]

### 假设全部接受，梯度更新

\[
51\times2048\times4
\approx418\text{K}
\]

总计大约：

\[
0.48\text{M}
\]

次运算/输出行。

相比基础增量 Schwarz 的约 9.04M：

\[
\frac{0.48}{9.04}\approx5.3\%
\]

所以即使处理 10% 的重叠 group，对包含初始化成本的完整 Schwarz 阶段，理论增量可能只有约 5%～8%。

## 重分组本身的成本

如果每次都扫描完整协方差矩阵寻找跨组相关性：

\[
O(D^2)
\]

对于 \(D=2048\) 不算特别巨大，但如果对每个输出行都做就很浪费。

正确做法是：

- \(H_q\) 对同一 Linear 的所有输出行共享；
- 通道 top-k 相关邻居只计算一次；
- 每个输出行只根据当前梯度从预计算邻居中选择；
- 不重新对完整 \(D^2\) 关系排序。

这样动态选择近似为：

\[
O(kD)
\]

其中 \(k\) 可以取 8 或 16。

---

# 三项合并后的总复杂度

可以写成：

\[
T_{\mathrm{new}}
=
T_{\mathrm{quant}}
+
T_{\mathrm{metric\ setup}}
+
T_{\mathrm{base\ Schwarz}}
+
T_{\mathrm{overlap}}
\]

对应理论复杂度：

\[
O(ND)
+
O(ND^2+NDR)
+
O(RD^2+RCbD)
+
O(pR(D^2+CbD))
\]

由于 \(b=4,C=81,p\ll1\)，简化为：

\[
O(ND^2+NDR+RD^2)
\]

如果 \(R\approx D\)，主要是：

\[
O(ND^2+D^3)
\]

但这里的 \(D^3\) 来自对 \(R\) 个输出行分别运行 \(D^2\) 级更新，不是对每个候选计算完整 \(D^2\)。

更重要的是，与当前方案比较：

- 第 1 项显著减少候选评价的常数；
- 第 2 项只增加一次或少量校准 GEMM；
- 第 3 项只增加 \(p\) 比例的 Schwarz 更新；
- 搜索空间仍然是每组 81 个，没有指数扩张。

---

# 对 180 秒基线的墙钟时间估算

无法仅凭渐进复杂度准确预测，需要知道当前 180 秒中 metric 评分占多少比例。可以使用 Amdahl 估算。

设：

- 当前 metric 候选评分占总时间比例为 \(f\)；
- 增量二次型让该部分加速 \(r\) 倍；
- 非对称 metric 增加 \(T_{\mathrm{asym}}\)；
- 重叠分组增加 \(T_{\mathrm{overlap}}\)。

那么：

\[
T_{\mathrm{new}}
\approx
180
\left[
(1-f)+\frac{f}{r}
\right]
+
T_{\mathrm{asym}}
+
T_{\mathrm{overlap}}
\]

假设增量评分实际加速 10 倍：

| metric 评分占比 | 第1项后估算 |
|---:|---:|
| 20% | \(180(0.8+0.02)=147.6s\) |
| 30% | \(180(0.7+0.03)=131.4s\) |
| 50% | \(180(0.5+0.05)=99s\) |
| 70% | \(180(0.3+0.07)=66.6s\) |

然后再增加：

- 非对称 metric：一次额外校准 GEMM；
- 5%～10% overlap：优化后 Schwarz 阶段的约 3%～8%；
- 分组构造：如果复用 top-k 相关关系，通常较小。

因此比较合理的三种情形是：

### 当前没有增量优化

加入三项后很可能仍在：

\[
100\sim200\text{ 秒}
\]

甚至比原来的 180 秒更快。

### 当前已有部分增量优化

第 1 项加速有限，加入非对称 metric 和 10% overlap 后可能为：

\[
190\sim240\text{ 秒}
\]

通常仍低于 300 秒。

### 当前已经完整使用同样的增量公式

第 1 项没有收益，只增加第 2、3 项，可能来到：

\[
210\sim270\text{ 秒}
\]

具体取决于校准 GEMM 的规模。

---

# 建议先做的三个计时点

不要一次完成三项后才判断超时。先分别记录：

1. `metric_setup_time`  
   构造 \(H\)、\(H_q\)、\(B\) 的时间。

2. `candidate_score_time`  
   81 候选评分的总时间。

3. `gradient_update_time`  
   接受候选后更新 \(g\) 的时间。

如果当前 `candidate_score_time` 很高，第 1 项价值最大。

如果当前时间主要消耗在：

\[
H=X^TX
\]

或大规模 GEMM 上，第 1 项对总时间帮助有限，非对称 metric 就需要谨慎。

# 推荐时间预算

在 300 秒限制下可以这样控制：

- 0～180 秒：原始流程或增量 Schwarz；
- 最多 40～60 秒：构造非对称 metric；
- 最多 30 秒：重叠 Schwarz；
- 270 秒停止所有新搜索；
- 30 秒留给编码、输出和异常余量。

重叠搜索不要固定处理 10%，而应设置双重条件：

\[
\text{processed ratio}<10\%
\]

并且：

\[
t_{\mathrm{elapsed}}<270s
\]

最终判断：

> 三项优化不会造成类似 \(81\rightarrow625\) 的指数级增长。第 1 项将搜索从“候选数 × 全维 metric”压缩为一次全局梯度维护加大量四维评分；第 2 项增加一次校准 GEMM；第 3 项按重叠比例线性增加。只要当前实现尚未使用精确增量公式，三项同时加入仍很可能比现有 180 秒更快。
