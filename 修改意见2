你的判断是对的。基线已经 180 秒，而上限是 300 秒，只剩 120 秒余量，**全量三级精确搜索风险很高，不建议直接做**。

更合适的是：

> 保留原来的快速贪心编码，只对少量“明显难量化”的块做局部精确搜索。

## 1. 可以搜索多少块

假设三级精确搜索的单块成本约为原方法的 8 倍，只对比例 \(p\) 的块使用搜索，则粗略耗时为：

\[
T_{\mathrm{new}}
\approx
180\left[(1-p)+8p\right]
=
180(1+7p)
\]

要求：

\[
180(1+7p)\le300
\]

得到：

\[
p\le 9.5\%
\]

所以在非常保守的估算下：

- 搜索前 5% 难块：约 243 秒；
- 搜索前 8% 难块：约 281 秒；
- 搜索前 10% 难块：约 306 秒，已经有超时风险。

实际中向量化以后可能没有 8 倍，但不应该预先依赖这一点。建议先对 1% 的块做计时，再动态确定搜索比例。

---

# 2. 推荐方案：难块触发的选择性精确搜索

所有块先走当前基线，同时计算一个很便宜的“难度分数”。

可以由以下指标构成。

## 基线量化误差

\[
e_b=
\frac{
\|x_b-\hat x_b\|_2^2
}{
\|x_b\|_2^2+\epsilon
}
\]

误差越大，越值得搜索。

## 最大值异常程度

\[
r_b=
\frac{\max |x_b|}
{\operatorname{RMS}(x_b)+\epsilon}
\]

\(r_b\) 很大，说明少数异常值可能拉高整个块的 scale。

## 最大值与次大值比例

\[
o_b=
\frac{x_{\max}}
{x_{\mathrm{second}}+\epsilon}
\]

如果最大值远高于次大值，Scale factor 和层级位的贪心选择更容易不合理。

## 量化为零的比例

\[
z_b=
\frac{\#\{\hat x_i=0\}}{64}
\]

如果大量非零值被量化成 0，说明公共 scale 可能过大。

## 饱和比例

\[
c_b=
\frac{\#\{|x_i/S|>q_{\max}\}}{64}
\]

饱和多说明 Scale factor 可能过小。

可以组合成：

\[
h_b=
\alpha e_b+
\beta r_b+
\gamma o_b+
\delta z_b+
\eta c_b
\]

然后只搜索 \(h_b\) 最大的 5%～8% 块。

第一版甚至不需要精心调权重：直接按基线归一化 MSE 排序，通常就已经是一个不错的筛选器。

---

# 3. 不做三级全搜索，只做“局部纠错”

与其为难块重新枚举所有候选，还可以从当前编码出发，只检查附近的状态。

当前编码为：

\[
(S,e^{(2)},e^{(3)},q)
\]

只生成以下邻域候选：

- E6M2 保持不变、向上一个格点、向下一个格点；
- 翻转误差最大的那个 lv2；
- 翻转误差最大的 1～2 个 lv3；
- Scale factor 只尝试当前值附近两个候选；
- S1P2 随新的 scale 和层级重新就近取整。

例如每个难块只检查：

1. 原始编码；
2. 总 scale 向下一级；
3. 总 scale 向上一级；
4. 最可疑 lv2 翻转；
5. 最可疑 lv3 翻转；
6. lv2 与对应 lv3 联合翻转。

只有 5～6 个候选，不需要完整束搜索。

这可以称为：

> greedy initialization + one-step local refinement。

它通常能获得精确搜索的大部分收益，但成本低得多。

---

# 4. lv3 优先，lv2 次之，E6M2 最谨慎

三级状态的影响范围不同：

- 一个 lv3 影响 4 个值；
- 一个 lv2 影响 8 个值；
- E6M2 影响全部 64 个值。

因此局部搜索可以按这个顺序：

1. 先检查 16 个 lv3 中最可疑的少数几个；
2. 再检查对应的 lv2；
3. 只有整个块误差很高时才搜索 E6M2。

这样可以避免对大量正常块重复重算全部 64 个值。

## 如何找“最可疑的 lv3”

对每个 4 元素组计算：

\[
e_{b,g}
=
\sum_{i\in g}(x_i-\hat x_i)^2
\]

只对误差最大的 1～2 个四元素组尝试翻转 lv3。

同样，对每个 8 元素组计算误差，只对最差的一个组搜索 lv2 与两个 lv3 的 8 种组合。

这样每个难块不需要处理全部 8 个组。

---

# 5. 更推荐的分层预算

可以设计成三档。

| 块类型 | 比例示例 | 方法 |
|---|---:|---|
| 简单块 | 90%～95% | 直接使用现有基线 |
| 中等难块 | 4%～8% | 局部 one-flip/邻域搜索 |
| 极难块 | 0.5%～1% | 固定若干 E6M2 候选后做三级精确搜索 |

例如：

- 92%：原方法；
- 7%：最多 6 个候选；
- 1%：比较完整的搜索。

这比“前 8% 全部做 8 倍搜索”更稳妥。

---

# 6. 使用逐级早停

搜索一个难块时，先计算基线损失：

\[
L_{\mathrm{base}}
\]

然后按成本从低到高尝试。

### 第一级：Scale factor 邻居

如果已经得到明显改善：

\[
\frac{L_{\mathrm{base}}-L_1}{L_{\mathrm{base}}}
>\tau_1
\]

例如改善超过 20%，就停止，不再搜索更多候选。

### 第二级：最差 lv3/lv2

只有第一级改善不明显时才进入。

### 第三级：完整搜索

只有同时满足以下条件才进入：

- 基线误差位于最高 1%；
- 局部搜索仍未明显改善；
- 剩余时间允许。

这种早停会让大部分难块只使用一两次额外计算。

---

# 7. 使用两阶段损失，避免每个候选都跑算子

如果每个候选都计算完整 Linear 或 Attention 输出，成本会很高。

建议：

## 第一阶段：便宜的局部损失剪枝

权重块可以使用：

\[
L_{\mathrm{local}}
=
\sum_i h_i(x_i-\hat x_i)^2
\]

其中 \(h_i\) 可以取 Hessian 对角线或输入通道能量：

\[
h_i=\sum_nX_{n,i}^2
\]

这比普通 MSE 更接近 Linear 输出误差，但仍然只需要逐元素计算。

每个难块先生成 5～8 个候选，只保留局部损失最好的两个。

## 第二阶段：昂贵的算子损失

只对两个最终候选计算：

\[
L_{\mathrm{linear}}
=
\|XW^T-\hat X\hat W^T\|^2
\]

Attention 则先用 logits 误差剪枝：

\[
L_{QK}
=
\|QK^T-\hat Q\hat K^T\|^2
\]

只有最终候选才计算 softmax 和 \(PV\)。

---

# 8. 用增量损失避免重复完整 GEMM

对于 Linear，如果只改变一个权重块：

\[
\hat W_b^{\mathrm{old}}
\rightarrow
\hat W_b^{\mathrm{new}}
\]

定义：

\[
\Delta W_b=
\hat W_b^{\mathrm{new}}
-
\hat W_b^{\mathrm{old}}
\]

不需要重新计算整个 Linear，只需计算该块导致的输出变化：

\[
\Delta Y=X_b\Delta W_b^T
\]

如果已经保存当前残差：

\[
R=Y_{\mathrm{ref}}-\hat Y
\]

新损失为：

\[
\|R-\Delta Y\|^2
\]

展开：

\[
L_{\mathrm{new}}
=
\|R\|^2
-2\langle R,\Delta Y\rangle
+\|\Delta Y\|^2
\]

这样可以用小块 GEMM 更新，而不是每个候选重新执行完整矩阵乘法。

如果已经计算 Hessian，还可以直接用块级二次型估算：

\[
\Delta L
\approx
2\langle E,H\Delta W\rangle
+
\operatorname{tr}(\Delta W H\Delta W^T)
\]

---

# 9. 向量化比减少理论操作更重要

固定 E6M2 后，一个 8 元素组只有 8 种 lv2/lv3 组合。不要写成 Python 三重循环：

```text
for block
  for group
    for candidate
```

更合适的是一次构造：

\[
[\text{num\_groups},8,8]
\]

形状的候选张量：

- 第一个 8：每个 64-value 块中的 8 个组；
- 第二个 8：8 种 lv2/lv3 组合；
- 最后一个 8：该组的 8 个数。

然后一次完成：

- scale；
- clamp；
- S1P2 映射；
- 反量化；
- loss；
- `argmin`。

这种实现虽然理论上评估了 8 倍候选，但在 GPU 上墙钟时间未必增加 8 倍。

不过要防止候选张量一次展开过大，应该按 chunk 批处理。

---

# 10. 设置硬时间预算

不要只根据块比例估算，最好让算法有实际的时间控制。

例如：

- 0～180 秒：基线和必要校准；
- 180～250 秒：中等难块局部搜索；
- 250～280 秒：极难块精确搜索；
- 280 秒：停止产生新候选；
- 280～300 秒：整理结果和最终输出。

搜索过程随时检查：

\[
t_{\mathrm{elapsed}}>t_{\mathrm{stop}}
\]

就返回当前最优合法 HiF4 编码。

这样即使不同数据组规模不同，也不会因为固定搜索比例导致超时。

---

# 11. 低成本替代优化

如果三级搜索仍然太贵，优先使用以下几项。

## Attention K 均值消除

复杂度：

\[
O(Nd)
\]

而 QK Attention 本身是：

\[
O(N^2d)
\]

因此相对开销很小，而且未量化 softmax 结果严格不变。这可能比昂贵的元数据搜索更划算。

## Bias compensation

校准阶段计算一次输出平均误差，在线只增加逐输出通道加法，成本很低。

## 固定 E6M2，只精确搜索最差 8 元素组

不重新搜索全局 scale，只对一个最差的 8 元素组枚举 8 种 lv2/lv3 组合。每个被选中的 64-value 块只多处理 8 个数，而不是反复处理全部 64 个数。

## 量化输入感知 GPTQ

如果 GPTQ、block diagonal 和 2-pass 已经存在，只需把重构目标从：

\[
XW^T\approx X\hat W^T
\]

改为：

\[
XW^T\approx \hat X\hat W^T
\]

通常不需要增加一个全新的大搜索流程。

---

# 最终建议

在 5 分钟约束下，我不建议全量三级精确/束搜索。推荐配置是：

1. 所有块先运行现有基线；
2. 用归一化量化误差筛选最差 5%～8% 块；
3. 其中大部分只搜索最差的一个 8 元素组；
4. 只对最高 0.5%～1% 的极难块搜索 E6M2 邻居和完整层级组合；
5. 候选先用 Hessian 对角加权误差剪枝；
6. 只对最终两个候选计算算子级误差；
7. 280 秒硬停止。

保守估计，如果完整搜索是基线单块成本的 8 倍，只搜索约 8% 的块理论上已经接近 281 秒；因此实际设置最好从 **5% 局部搜索 + 0.5% 精确搜索** 开始，而不是直接冲到 8% 全精确搜索。
