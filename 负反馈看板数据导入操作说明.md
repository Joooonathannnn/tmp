# 负反馈轨迹看板：数据准备与导入操作说明

## 1. 这份说明是给谁看的

这份说明用于指导模型或操作人员给 `negative-feedback-dashboard.html` 准备数据并完成导入。

执行任务时必须严格按照本文档操作，不要自行修改字段名，不要改变 JSON 层级，不要把项目写成数组。

## 2. 最终需要得到什么

最终需要准备两个文件：

1. `negative-feedback-dashboard.html`：已经完成的看板页面，不需要修改。
2. 一个 `.json` 数据文件，例如 `负反馈轨迹数据.json`：需要按照本文档生成。

正确导入后，看板会自动展示：

- 平台收集的负反馈轨迹总量。
- 轨迹覆盖的自然日数量。
- 每日轨迹数量趋势。
- 根因类别分布。
- 关联项目分布。
- 每条负反馈的完整内容、根因分析和改进建议。
- 每条负反馈唯一关联的项目及项目数据映射。

## 3. 必须遵守的规则

1. 不要修改 `negative-feedback-dashboard.html` 中的代码。
2. 真实数据必须保存为一个 UTF-8 编码的 JSON 文件。
3. JSON 顶层必须是一个对象，不能直接写成数组。
4. 顶层对象中必须包含 `records` 数组。
5. `records` 中的每个对象代表一条负反馈轨迹。
6. 一条负反馈轨迹必须且只能关联一个项目。
7. `project` 必须是一个对象，禁止写成数组。
8. `project.dataMapping` 必须是一个对象，禁止写成字符串或数组。
9. 时间统一使用 ISO 8601 格式，例如 `2026-08-14T09:30:00+08:00`。
10. 每条轨迹的 `id` 应保持唯一，不要让两条轨迹使用同一个 ID。
12. 根因分析和改进建议由数据提供，看板不会自动生成这些内容。
13. 不知道可选字段内容时可以填写空字符串，但必填字段禁止为空。
14. JSON 中最后一个字段后面不能多写逗号。
15. JSON 只能使用英文双引号 `"`，不能使用中文引号。

## 4. 最简单的操作方法

### 方法一：从页面下载模板

1. 双击打开 `negative-feedback-dashboard.html`。
2. 点击页面顶部的“下载模板”。
3. 浏览器会下载 `负反馈轨迹数据模板.json`。
4. 使用文本编辑器打开模板文件。
5. 按照真实数据修改模板。
6. 保存文件，编码选择 UTF-8。
7. 返回看板，点击“导入 JSON”。
8. 选择修改后的 JSON 文件。

### 方法二：模型直接生成 JSON

如果由模型处理数据，模型应该直接生成一个完整的 JSON 文件，不要生成 Markdown 表格，不要只输出部分字段，也不要把 JSON 包在解释文字中。

## 5. 完整且有效的 JSON 示例

以下内容可以直接保存为 `.json` 文件并导入：

```json
{
  "datasetName": "2026年8月负反馈轨迹分析",
  "generatedAt": "2026-08-14T10:00:00+08:00",
  "records": [
    {
      "id": "NF-001",
      "timestamp": "2026-08-14T09:30:00+08:00",
      "source": "Android",
      "feedbackSummary": "首页指标一直转圈",
      "feedbackContent": "用户打开经营首页后，核心指标持续显示加载状态，刷新页面后仍然没有数据。",
      "rootCauseCategory": "数据加载",
      "rootCauseAnalysis": "高峰期聚合接口响应时间超过前端超时阈值，同时页面没有命中缓存。",
      "improvementSuggestion": "为核心指标增加短时缓存和分级超时提示，并增加接口P95耗时监控。",
      "project": {
        "id": "P-001",
        "name": "经营分析看板",
        "module": "核心指标",
        "owner": "数据产品组",
        "version": "2.4.0",
        "dataMapping": {
          "api": "/metrics/summary",
          "event": "dashboard_open",
          "metricField": "daily_amount"
        }
      }
    },
    {
      "id": "NF-002",
      "timestamp": "2026-08-14T11:15:00+08:00",
      "source": "iOS",
      "feedbackSummary": "筛选条件会被重置",
      "feedbackContent": "用户从详情页返回列表后，原来选择的日期和部门筛选条件全部丢失。",
      "rootCauseCategory": "交互体验",
      "rootCauseAnalysis": "筛选状态只保存在列表组件内部，页面路由切换后组件被销毁。",
      "improvementSuggestion": "将筛选条件同步到URL参数，并在用户返回列表时自动恢复。",
      "project": {
        "id": "P-001",
        "name": "经营分析看板",
        "module": "筛选器",
        "owner": "数据产品组",
        "version": "2.4.0",
        "dataMapping": {
          "page": "dashboard_list",
          "route": "/dashboard"
        }
      }
    }
  ]
}
```

## 6. 顶层字段说明

| 字段 | 类型 | 是否必需 | 说明 | 示例 |
|---|---|---|---|---|
| `datasetName` | 字符串 | 建议填写 | 页面右上角显示的数据集名称 | `2026年8月负反馈轨迹分析` |
| `generatedAt` | 字符串 | 建议填写 | 数据生成或更新时间，建议使用 ISO 时间 | `2026-08-14T10:00:00+08:00` |
| `records` | 数组 | 必需 | 所有负反馈轨迹的数组 | `[{...}, {...}]` |

正确的顶层结构：

```json
{
  "datasetName": "数据集名称",
  "generatedAt": "2026-08-14T10:00:00+08:00",
  "records": []
}
```

错误的顶层结构：

```json
[
  { "id": "NF-001" }
]
```

错误原因：顶层直接写成了数组，没有 `records` 字段。

## 7. 每条负反馈轨迹的字段说明

| 字段 | 类型 | 是否必需 | 说明 | 示例 |
|---|---|---|---|---|
| `id` | 字符串 | 必需 | 轨迹唯一编号 | `NF-001` |
| `timestamp` | 字符串 | 必需 | 轨迹发生时间，使用 ISO 格式 | `2026-08-14T09:30:00+08:00` |
| `source` | 字符串 | 建议填写 | 轨迹来源或平台 | `Android`、`iOS`、`H5` |
| `feedbackSummary` | 字符串 | 必需 | 简短的负反馈标题 | `首页指标一直转圈` |
| `feedbackContent` | 字符串 | 必需 | 完整负反馈内容 | `用户打开页面后一直没有数据` |
| `rootCauseCategory` | 字符串 | 必需 | 根因分类，用于图表统计 | `数据加载` |
| `rootCauseAnalysis` | 字符串 | 必需 | 对问题原因的完整分析 | `接口响应超过超时阈值` |
| `improvementSuggestion` | 字符串 | 必需 | 可以执行的改进建议 | `增加缓存和超时监控` |
| `project` | 对象 | 必需 | 该轨迹唯一关联的项目 | `{ "id": "P-001", ... }` |

## 8. 项目对象的字段说明

| 字段 | 类型 | 是否必需 | 说明 | 示例 |
|---|---|---|---|---|
| `project.id` | 字符串 | 必需 | 项目唯一编号 | `P-001` |
| `project.name` | 字符串 | 必需 | 项目名称 | `经营分析看板` |
| `project.module` | 字符串 | 建议填写 | 问题所属模块 | `核心指标` |
| `project.owner` | 字符串 | 建议填写 | 项目负责人或负责团队 | `数据产品组` |
| `project.version` | 字符串 | 建议填写 | 问题对应版本 | `2.4.0` |
| `project.dataMapping` | 对象 | 必需 | 轨迹和项目数据之间的字段映射 | `{ "event": "dashboard_open" }` |

### 项目映射的正确写法

```json
"project": {
  "id": "P-001",
  "name": "经营分析看板",
  "module": "核心指标",
  "owner": "数据产品组",
  "version": "2.4.0",
  "dataMapping": {
    "traceProjectId": "P-001",
    "event": "dashboard_open",
    "api": "/metrics/summary"
  }
}
```

### 项目映射的错误写法一

```json
"project": [
  { "id": "P-001", "name": "经营分析看板" }
]
```

错误原因：`project` 被写成数组。一条轨迹只能关联一个项目，所以这里必须是对象。

### 项目映射的错误写法二

```json
"dataMapping": "event=dashboard_open"
```

错误原因：`dataMapping` 被写成字符串。它必须是 JSON 对象。

## 9. 把原始数据转换为 JSON 的固定步骤

模型处理原始数据时，必须按以下顺序执行：

1. 找出每一条独立的负反馈轨迹。
2. 每条原始轨迹生成一个 `records` 元素。
3. 为每条轨迹生成唯一 `id`，例如 `NF-001`、`NF-002`。
4. 将发生时间转换为 ISO 8601 格式。
5. 填写简短摘要和完整负反馈内容。
6. 从数据中读取已经提供的根因类别、根因分析和改进建议。
7. 不要自行编造根因分析或改进建议；原始数据缺失时应报告缺失，不能伪造。
8. 找到这条轨迹唯一关联的项目。
9. 将项目写入一个 `project` 对象。
10. 将轨迹字段与项目字段的对应关系写进 `project.dataMapping` 对象。
11. 将全部轨迹放入顶层 `records` 数组。
12. 检查 JSON 语法。
13. 保存为 UTF-8 编码的 `.json` 文件。

禁止把多条负反馈合并成一条记录。禁止把一条负反馈拆成多个项目记录。

## 10. 在页面中导入 JSON

1. 使用 Chrome 或 Edge 双击打开 `negative-feedback-dashboard.html`。
2. 点击页面顶部的“导入 JSON”。
3. 选择准备好的 `.json` 文件。
4. 如果导入成功，页面右下角会显示“已导入 X 条轨迹”。
5. 页面顶部统计卡、图表、筛选器和轨迹列表会自动更新。
6. 点击列表中的“查看”，检查根因分析、改进建议和项目数据映射。

导入行为只发生在当前浏览器页面中，不会修改原来的 HTML 文件，也不会把数据上传到服务器。

页面刷新或关闭后，已导入数据不会永久保存。再次打开页面时，需要重新导入 JSON。

## 11. 常见错误及处理方法

### 错误：数据顶层必须包含 records 数组

原因：顶层没有 `records`，或者 `records` 不是数组。

修复：

```json
{
  "records": []
}
```

### 错误：某条轨迹缺少字段 timestamp

原因：对应记录没有 `timestamp`，或者该字段为空。

修复：补充有效时间，例如：

```json
"timestamp": "2026-08-14T09:30:00+08:00"
```

### 错误：某条轨迹的时间格式无效

原因：时间无法被浏览器识别。

修复：统一改为 ISO 8601 格式，不要只写“昨天”“上午九点”等模糊时间。

### 错误：必须且只能关联一个项目对象

原因：没有 `project`、`project` 是 `null`，或者 `project` 被写成数组。

修复：为该轨迹写入一个项目对象。

### 错误：项目缺少 id 或 name

原因：`project.id` 或 `project.name` 为空。

修复：补充项目编号和项目名称。

### 错误：项目数据映射必须为对象

原因：`dataMapping` 缺失，或被写成字符串、数组、`null`。

如果确实没有映射字段，也必须写成空对象：

```json
"dataMapping": {}
```

### 错误：JSON 无法解析

重点检查：

- 是否使用英文双引号。
- 字段之间是否有逗号。
- 最后一个字段后面是否多写了逗号。
- 大括号和中括号是否成对闭合。
- 文本中包含双引号时是否写成 `\"`。
- 是否写入了 JSON 不支持的值，例如 `NaN`、`Infinity`、Python 的 `None`。

## 12. 导入后的验收清单

导入完成后逐项检查：

- [ ] 页面右上角的数据集名称正确。
- [ ] 负反馈轨迹总量等于 `records` 数组长度。
- [ ] 覆盖天数等于所有 `timestamp` 日期去重后的数量。
- [ ] 每日轨迹趋势图有数据。
- [ ] 根因类别分布与 `rootCauseCategory` 一致。
- [ ] 关联项目分布与 `project.name` 一致。
- [ ] 日期、根因类别和项目筛选可以正常使用。
- [ ] 每条轨迹的“查看”按钮可以打开详情。
- [ ] 详情中的负反馈内容、根因分析和改进建议正确。
- [ ] 每条轨迹只显示一个关联项目。
- [ ] 项目数据映射内容正确。
- [ ] 页面没有显示“导入失败”。

只要有一项不符合，就不能宣布数据导入完成。

## 13. 可以直接交给模型的任务指令

将下面这段内容连同原始数据和本说明一起交给模型：

```text
你的任务是为 negative-feedback-dashboard.html 生成可导入的 JSON 数据文件。

必须完整阅读《负反馈看板数据导入操作说明.md》，严格按照其中的数据结构执行。

要求：
1. 不要修改 HTML 文件。
2. 顶层必须是对象，并包含 records 数组。
3. 一条原始负反馈对应 records 中的一条记录。
4. 每条记录必须且只能包含一个 project 对象。
5. timestamp 必须转换为 ISO 8601 格式。
6. 根因分析和改进建议只能使用原始数据已经提供的内容，不得编造。
8. project.dataMapping 必须是对象。
9. 输出必须是合法的纯 JSON，不要在 JSON 前后添加解释文字。
10. 输出前逐条检查必填字段，并核对 records 数量。

如果原始数据缺少必填字段，不要猜测；列出缺少的记录ID和字段，并停止生成最终JSON。
```

## 14. 后续接入服务器时的说明

当前页面通过本地 JSON 文件加载数据。以后接入服务器时，应保持本文档中的 JSON 结构不变。

服务器接口应返回同样的顶层结构：

```json
{
  "datasetName": "数据集名称",
  "generatedAt": "2026-08-14T10:00:00+08:00",
  "records": []
}
```

只替换数据读取方式，不要重新设计统计卡、图表、筛选器、列表或详情的数据字段。
